package com.vprok.forms.entity;

/** Mirrors the chk_attr_data_type CHECK constraint on attribute_definition.data_type. */
public enum AttributeDataType {
    BOOLEAN,
    STRING,
    INTEGER,
    DECIMAL,
    DATE
}
